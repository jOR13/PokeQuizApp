<nav class="bg-pokemonYellow p-4 flex justify-between items-center shadow-md">
  <div class="flex items-center">
    <%= link_to root_path, class: "pokemon-style" do %>
      PokéQuiz
    <% end %>
  </div>
  <div class="flex items-center space-x-2 ml-2">
    <%= link_to api_players_path, class: 'bg-green-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded-full flex items-center space-x-2', target: '_blank' do %>
      <i class="fas fa-code"></i>
      <span class="hidden md:inline"><%= t('views.navbar.api') %></span>
    <% end %>

    <div data-controller="language" class="relative">
      <button id="language-toggle" data-action="click->language#toggle" class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded-full flex items-center space-x-2">
        <i class="fas fa-globe"></i>
        <span class="hidden md:inline"><%= t('views.navbar.select_language') %></span>
      </button>
    </div>

    <button data-action="click->audio#toggle" class="bg-red-500 hover:bg-red-700 text-white font-bold py-2 px-4 rounded-full flex items-center space-x-2" id="music-toggle">
      <i id="music-icon" class="fas fa-volume-up"></i>
      <span class="hidden md:inline"><%= t('views.navbar.music') %></span>
    </button>
  </div>
</nav>
